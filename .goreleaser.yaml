version: 2

project_name: packshift

builds:
    - main: ./main.go
      goos:
          - windows
      goarch:
          - amd64
          - arm64

archives:
    - formats: ["zip"]
      name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
